# Docs

This directory is reserved for project documentation that should be easy to
discover from the repository root.

Layout:

- `development/plans/app/` - implementation notes and app-level planning docs

Vendor-specific build instructions, patch notes, and status documents stay next
to the vendored code they describe, such as `vendor/wllama/`.
